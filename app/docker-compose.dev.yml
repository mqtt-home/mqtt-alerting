services:
  mqtt-mail:
    build:
      context: .
      dockerfile: Dockerfile
    image: pharndt/mqtt-mail:latest
    ports:
      - "8080:8080"
      - "6060:6060"
    volumes:
      - ../production/config:/var/lib/mqtt-mail
    environment:
      TZ: Europe/Berlin
    restart: unless-stopped
